<UserControl x:Class="PrismDemo.Tasks.Views.TaskListItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Border BorderBrush="SteelBlue" BorderThickness="2">
            <StackPanel Orientation="Vertical" Background="Purple">
                <TextBlock Text="{Binding Title}" Foreground="White"/>
                <TextBlock Text="{Binding Description}" Foreground="White"/>
                <TextBlock Text="{Binding Priority}" Foreground="White"/>
                <TextBlock Text="{Binding Id, Mode=OneWay}" Foreground="White"/>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
